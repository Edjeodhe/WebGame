name = "insect-king-api"
main = "src/index.js"
compatibility_date = "2024-11-01"

# 세이브 데이터를 저장할 KV 네임스페이스.
# 아래 id는 최초 1회 `wrangler kv namespace create SAVES_KV` 실행 후
# 나온 실제 id로 교체해야 한다. (README의 "Cloudflare 백엔드 설정" 참고)
[[kv_namespaces]]
binding = "SAVES_KV"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
